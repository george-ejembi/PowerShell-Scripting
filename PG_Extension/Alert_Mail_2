function Send-EmailAlert {
    param($Subject, $Body)

    Send-MailMessage `
        -From "pipeline@company.com" `
        -To "team@company.com" `
        -Subject $Subject `
        -Body $Body `
        -SmtpServer "smtp.server.com"
}
